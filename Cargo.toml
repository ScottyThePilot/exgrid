[package]
name = "exgrid"
version = "0.1.0"
edition = "2021"

[dependencies]
num-traits = "0.2.15"
rayon = { version = "1.5.3", optional = true }

[dev-dependencies]
rand = "0.8.5"

[features]
default = []
automata = []
multi-thread = ["dep:rayon"]
